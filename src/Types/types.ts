export type User = {
  name: string;
  age: string;
  userName: string;
  userEmail: string;
  phoneNumber: string;
  password: string;
  userId: number;
};
export type TodosType = {
  desc: string;
  id: number;
  isComplete: boolean;
};
